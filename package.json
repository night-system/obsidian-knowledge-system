{
  "name": "obsidian-knowledge-system",
  "version": "0.8.0",
  "description": "配置驱动的 AI 知识系统框架（v0.8.0）：聊天/设置界面图标彻底修复（lucide 图标名实测可用 + setIcon 可见性兜底，任何主题可见）+ 预设 UI 布局调整（工具启用开关移到折叠头部右侧不展开也能开关、chevron 移左、删除键移右分离）。v0.7.0 预设可折叠 + 每工具独立折叠配置 + update_note_yaml 阉割版 + create_note 标题模板；v0.6.0 聊天 UI 一比一复刻 dsh + 修复渲染两份 + 日志复制按钮；v0.5.0 流式期间直接渲染 markdown + TTFT 检测点 + 工具预设系统；Base URL 端点自动探测、错误可复制诊断、AI 聊天视图（原生 fetch 流式，失败自动降级非流式）、内置工具集、测试按钮。",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production"
  },
  "keywords": [
    "obsidian",
    "plugin",
    "knowledge-system"
  ],
  "author": "night-system",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "builtin-modules": "^4.0.0",
    "esbuild": "^0.21.5",
    "obsidian": "latest",
    "tslib": "^2.6.0",
    "typescript": "^5.4.0"
  }
}
